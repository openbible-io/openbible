{
	"devDependencies": {
		"@types/bun": "^1.2.4",
		"@biomejs/biome": "^1.9.4",
		"typescript": "^5.8.2"
	},
	"workspaces": ["shareword", "core", "dev-server", "bibles/*"],
	"scripts": {
		"build": "bun --filter '*' build",
		"download": "bun --filter '*' download",
		"site": "bun --filter '*' site",
		"clean": "bun --filter '*' clean"
	}
}
